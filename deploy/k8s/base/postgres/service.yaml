apiVersion: v1
kind: Service
metadata:
  name: my-test-project-database
spec:
  clusterIP: None
  selector:
    app: my-test-project-database
  ports:
    - port: 5432
      targetPort: 5432
---
apiVersion: v1
kind: Service
metadata:
  name: my-test-project-database-svc
spec:
  selector:
    app: my-test-project-database
  ports:
    - port: 5432
      targetPort: 5432
